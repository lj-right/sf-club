spring:
  application:
    name: sf-club-subject
  profiles:
      active: dev
  cloud:
    nacos:
      config:
        server-addr: 27.106.112.214:8848
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        file-extension: yaml
      discovery:
        enabled: true
        server-addr: 27.106.112.214:8848