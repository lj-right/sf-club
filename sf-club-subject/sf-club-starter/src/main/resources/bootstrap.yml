spring:
  application:
    name: sf-club-subject
  profiles:
      active: dev
  cloud:
    nacos:
      config:
        server-addr: 8.148.214.12:8848
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace:
        file-extension: yaml
      discovery:
        enabled: true
        server-addr: 8.148.214.12:8848